name: Deploy - SIT (IIS)

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["CI - Build, Test, Scan"]
    types: [completed]
    branches: ["release"]

permissions:
  contents: read
  actions: read

jobs:
  deploy-sit:
    name: Deploy to SIT (IIS)
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    runs-on: [self-hosted, windows]
    environment:
      name: sit

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download build artifact
        id: download-artifact
        uses: actions/download-artifact@v4
        with:
          name: angular-dist
          path: ./dist
          run-id: ${{ github.event.workflow_run.id || github.run_id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true

      - name: Build Angular (fallback if artifact absent)
        if: steps.download-artifact.outcome != 'success' || !hashFiles('./dist/**')
        working-directory: angular.web
        run: |
          npm ci
          npm run build -- --configuration production

      - name: Deploy Angular to IIS
        shell: powershell
        run: |
          ./scripts/deploy-iis.ps1 `
            -ArtifactPath "./dist/angular.web/browser" `
            -SitePath "${{ vars.IIS_SITE_PATH || 'C:\inetpub\wwwroot\web' }}" `
            -AppPoolName "${{ vars.IIS_APP_POOL || 'AngularWebAppPool' }}" `
            -SiteName "${{ vars.IIS_SITE_NAME || 'Default Web Site' }}" `
            -HealthCheckUrl "${{ vars.HEALTH_CHECK_URL || 'http://localhost/' }}" `
            -EnvironmentName "SIT"
